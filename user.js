import { auth, db } from './firebase-config.js';
// this file intentionally left small; app.js handles user panel in SPA flow
console.log('user.js loaded');
window.addEventListener('DOMContentLoaded', ()=>{ /* placeholder */ });
